# Known Gaps

| Priority | Domain | Issue | Why it matters | Recommended next action | Blocking |
| --- | --- | --- | --- | --- | --- |
| P1 | EAS preview build | EAS project `@karlcupid/corneriq` is linked in `app.json`. Android `preview` build `d550e9bb-b705-41a3-bae7-76c2b6d38453` failed in Gradle/Hermes because the floating Supabase dependency resolved to a Hermes-incompatible dynamic import. Supabase is now pinned to `2.50.0`, Expo/Metro checks pass, and fresh cache-cleared build `c21c5692-011e-4c85-949f-355d0e1f753f` finished with APK artifact `https://expo.dev/artifacts/eas/pYeMLGCyyhfB72dRYhG93K.apk`. | CornerIQ now has a preview artifact, but broad distribution still needs private tester controls, physical-device checks, and metadata acceptance. | Record the artifact in release handoff, distribute only through a private tester channel, and complete release-owner physical-device/store-metadata checks before inviting testers. | Not blocking local audit completion; blocking broad distributed beta rollout until release-owner distribution controls are confirmed. |
| P1 | Boxer beta user testing | First rough Windows/Chrome browser test found onboarding and Today first-run clarity issues; this pass addressed the confusing labels, raw fields, weekly anchor framing, quick-log scale copy, and Start here guidance. | IA and friction choices still need validation under real training-day fatigue, weigh-in pressure, mobile keyboards, and quick-log interruptions. | Rerun the first-time browser test from sign-up through Today, then run guided mobile beta sessions on Today, Fuel Command, Train Workout, Plan Week/Next Week/Adjustments, Profile Data/Audit, and the fast workout completion path. | Blocking for confident beta polish, not for current local smoke. |
| P1 | Coach activation boundary | The Edge Function now requires a Bearer JWT, verifies the caller with Supabase Auth, and only lets the athlete activate a pending relationship, but production audit logging/admin policy and UI gating are still incomplete. | Coach-only commands must not ship broadly until active status, consent, permissions, and audit behavior are proven end to end. | Add audit events, deployed function tests, admin/team policy, and athlete-facing consent copy before exposing coach UI. | Blocking for shared coach accounts. |
| P1 | Permissioned nutrition review workflow | Nutrition safety reviews now persist rows/events, athlete acknowledgement, and athlete-facing history panels, but there is no coach/clinician/dietitian reviewer UI, no messaging, and no exposed reviewer-clear workflow. | Hard stops are visible, persisted, and cannot be self-cleared, but real review operations still need permissioned reviewer identity, relationship policy, and audit behavior. | Add reviewer permissions through coach/clinician relationship policy or a trusted server-side function before exposing `cleared_by_reviewer`. | Blocking for production review workflow, not for current safety copy. |
| P1 | Remote release evidence | Local gates, stricter coverage thresholds, release-quality workflow/script, exact-SHA docs, current-SHA Quality/CodeQL evidence, evidence registry coverage, incident triage runbook, and migration `010` static tests are in place, but remote Supabase migration `010` is pending, live smoke is blocked, and Release Quality cannot pass for current candidate `7ff2d7f524c0c50075a429163e62dd8ce4b99419`. | Local production-quality readiness is stronger, but release evidence must prove the current remote schema and live path before handoff. | Apply or verify remote migration `010`, rerun migration list/dry-run until aligned, run live smoke with non-secret evidence, rerun `npm run release:quality`, and update `docs/27_RELEASE_EVIDENCE_LEDGER.md`. | Blocking for production/beta release evidence; not blocking local code audit. |
| P1 | Team memberships | `coach_team_memberships` was deferred. | A coach/team model will eventually need more than one-to-one athlete-coach rows. | Add team membership tables only after the one-to-one active relationship and permission model is proven. | Blocking for team workflows, not athlete-only use. |
| P2 | Numeric load progression | Free-text `loadText` is summarized but not parsed for progression. | Avoids fake progress and unsafe assumptions, but limits strength progression precision. | Add structured load fields later; keep free-text as notes until then. | Non-blocking. |
| P2 | Generated-session catalog depth | Accepted previews now materialize conservative future support sessions, but the mapping intentionally uses a small safe family set. | The durable future week is workout-ready, but advanced support variety should not be added without more catalog validation. | Expand family-specific templates only with prohibited-term tests, hard-day-cap tests, and no numeric load inference. | Non-blocking. |
| P2 | Mobile UX polish | Onboarding, Today, and quick logs now have clearer first-run copy, labels, examples, weekly anchor chips, and success feedback, but they still need hands-on mobile validation. | Beta users will interact on phones between sessions; dense input flows can reduce logging consistency. | Observe beta users, then split the densest forms into sheets/steppers only if real friction remains, without moving logic into screens. | Non-blocking. |
| P2 | Agent QA loop limitations | Approved local Playwright agent QA now covers first launch, full onboarding, Today, Fuel, Train, Plan, Profile Audit, Profile Data controls, error/recovery source checks, engine-output review, page-text snapshots, deterministic analysis, contact sheet, and a shareable bundle without Supabase credentials. It still uses deterministic local E2E state and does not validate live auth, real account data, email confirmation, physical devices, distribution artifacts, or human safety interpretation. | The harness reduces manual documentation work, but it must not be mistaken for real beta validation or release approval. | Use `cmd /c npm run qa:agent:ci` for repeatable evidence, share `qa-artifacts/corneriq-agent-qa-bundle.zip` for AI review, then schedule human review for live Supabase auth, physical mobile testing, boxing safety copy, cycle privacy, and release-owner distribution checks. | Non-blocking for local audit automation; blocking for release confidence if used as the only QA evidence. |
| P2 | Beta feedback triage | Beta feedback reports now persist as user-owned rows and a private incident triage runbook exists, but there is no production issue triage dashboard and feedback reports are not admin-reviewed in app. | Real beta testing can capture and privately triage reports, but status management still requires manual database review or a future permissioned workflow. | Use `docs/qa/INCIDENT_TRIAGE_RUNBOOK.md` for private beta handling; add a permissioned triage workflow only after admin/reviewer boundaries and privacy rules are defined. | Non-blocking for structured beta sessions. |
| P2 | Production release operations | Local release operations, CI quality checks, Release Quality workflow/script, app recovery, scenario QA docs, beta release-candidate checklist, EAS distribution docs, EAS profiles, beta preflight, coverage, focused smoke, agent QA, production quality audit, exact-SHA ledger, static safety scans, and a successful EAS preview artifact are in place. | Structured beta can run more repeatably, but production rollout still needs remote migration `010`, live smoke, store metadata, and owner-run mobile checks. | Run the manual Release Quality workflow after Supabase migration `010` is aligned and live smoke is recorded; keep mobile distribution in the release-owner lane. | Non-blocking for local structured beta sessions; blocking for distributed beta/release evidence. |
| P2 | App distribution assets | App name, slug, version, bundle identifier, Android package, EAS project link, EAS profiles, and a preview APK artifact exist, but app icon, splash, store metadata, tester distribution ownership, and physical-device acceptance have not been completed. | Preview beta can be prepared, but wider distribution needs recognizable app assets, owner-managed build credentials, and private tester communication. | Have the release owner confirm beta-only metadata, private tester channel, and physical-device behavior before inviting testers. | Non-blocking until external tester invites are sent. |
| P2 | External analytics | No external analytics or event pipeline is connected. | Privacy is simpler for beta, but aggregate usage patterns are not available outside smoke/tests and direct feedback. | Decide whether privacy-safe analytics are needed after guided beta sessions prove the core flows. | Non-blocking. |
| P2 | Routed drilldowns | Fuel, exercise, block, and audit histories are clearer panels, but there are no routed drilldown screens yet. | Panels are enough for beta comprehension, but longer histories need dedicated space and comparison views. | Add routed, view-model-driven Fuel history, exercise history, block history, and audit screens after user testing. | Non-blocking. |
| P2 | Plan adjustment UX | Plan controls are sectioned and athlete-only in UI; copy now frames buttons as engine requests and rejected/review-needed results show risk explanations. Coach move controls are not exposed and drag/drop remains deferred. | The command model is safer, but repeated weekly planning may still need clearer interaction design after human testing. | Add date picking or a safer move-session flow only after coach permissions, preview persistence, and beta findings justify it. | Non-blocking. |
| P2 | Scheduled/background roll-forward | App refresh now auto-materializes accepted previews at the boundary, but no server-scheduled job exists for athletes who do not open the app that day. | App-refresh automation covers normal use, but a server job may later improve durability for long gaps between sessions. | Decide whether to add a scheduled Supabase job after production audit/event policy is settled. | Non-blocking. |
| P2 | Nutrition command snapshot table | Fuel command snapshots are persisted through existing `nutrition_targets.target_payload`; review rows now have dedicated tables, but command snapshots still do not. | Current audit is adequate, but deeper command diff/history views would benefit from a purpose-built table separate from nutrition targets. | Add additive migration only when command history or reporting requires queryable nutrition-command records. | Non-blocking. |
| P2 | Food logging depth | Food logging remains quick manual macro/fiber/sodium entry with grouped Fuel history only. Barcode scanning, full meal planning, and a detailed food database remain intentionally deferred. | Command confidence can improve from logs, but athletes cannot yet inspect routed detailed meal history or use a food database. | Add routed manual Fuel history only after review permissions settle; keep barcode scanning and meal planning deferred. | Non-blocking. |
| P2 | Completion UX density | Workout detail now has a fast "Complete without exercise details" path and clearer helper copy, but the full detail panel is still dense on mobile. | The flow is functional but can feel heavy during post-workout logging. | Observe whether the fast path is enough; split into a lightweight sheet or stepper later while preserving `prescribed_only` defaults. | Non-blocking. |
| P2 | Cycle longitudinal UX | Cycle effects are explicit in training decisions, next-week preview blockers, and weekly summary flags, but history remains card-level. | Symptom-aware adjustments need better longitudinal review without fertility assumptions. | Add private trend summaries driven by cycle engine outputs. | Non-blocking. |
| P3 | Calendar polish | Plan has no drag/drop or calendar package. | Weekly command center is sectioned and readable but not yet a full planner. | Add after preview materialization, coach permissions, permissioned adjustment commands, and boxer user testing are hardened. | Non-blocking. |
| P3 | Export format | Export/delete controls include training block persistence and progression tables, but export is still a preview/count flow. | Users will eventually need a portable artifact. | Add downloadable export bundle after data model settles. | Non-blocking. |
