# Known Gaps

| Priority | Domain | Issue | Why it matters | Recommended next action | Blocking |
| --- | --- | --- | --- | --- | --- |
| P1 | Next-week preview persistence | Persisted progression decisions now materialize into a read-only next-week preview, but the preview is not stored in Supabase. | Auditors can see the current engine output, but there is no historical projection table with input/output hashes yet. | Add `training_next_week_previews` with owner RLS, input/output hashes, preview payload, repository methods, and live smoke coverage. | Non-blocking for UI preview, blocking for durable preview audit history. |
| P1 | Coach activation boundary | Migration 006 adds athlete-coach relationships and conservative RLS, but clients cannot activate relationships by design. | Coach-only commands must not ship until active status is granted by a trusted server-side path. | Add a server-side approval/function flow for active relationship status and scoped permissions before exposing coach UI. | Blocking for shared coach accounts. |
| P1 | Team memberships | `coach_team_memberships` was deferred. | A coach/team model will eventually need more than one-to-one athlete-coach rows. | Add team membership tables only after the one-to-one active relationship and permission model is proven. | Blocking for team workflows, not athlete-only use. |
| P2 | Numeric load progression | Free-text `loadText` is summarized but not parsed for progression. | Avoids fake progress and unsafe assumptions, but limits strength progression precision. | Add structured load fields later; keep free-text as notes until then. | Non-blocking. |
| P2 | Block history navigation | Plan now has a block history detail panel, but there is no routed drill-down screen or comparison view. | Long blocks will need more space for week-by-week audit and adjustment history. | Add a dedicated view-model-driven block history screen after preview persistence settles. | Non-blocking. |
| P2 | Exercise history navigation | Train now has a lightweight exercise-history panel, but no full exercise drill-down. | Athletes can see recent flags and counts, but cannot inspect a single exercise over time. | Add a dedicated exercise-history detail screen after structured load fields are designed. | Non-blocking. |
| P2 | Plan adjustment UX | Plan controls are intentionally skeletal and athlete-only in UI; coach move controls are not exposed. | The command model is safer, but repeated weekly planning still needs clearer interaction design. | Add clearer feedback, date picking, and safer move-session flow after coach permissions and preview persistence are hardened. | Non-blocking. |
| P2 | Completion UX density | Workout detail is still dense on mobile. | The flow is functional but can feel heavy during post-workout logging. | Split completion into a lightweight sheet or stepper while preserving `prescribed_only` defaults. | Non-blocking. |
| P2 | Cycle longitudinal UX | Cycle effects are explicit in training decisions, next-week preview blockers, and weekly summary flags, but history remains card-level. | Symptom-aware adjustments need better longitudinal review without fertility assumptions. | Add private trend summaries driven by cycle engine outputs. | Non-blocking. |
| P3 | Calendar polish | Plan has no drag/drop or calendar package. | Weekly command center is readable but not yet a full planner. | Add after multi-week block lifecycle, preview persistence, and permissioned adjustment commands are hardened. | Non-blocking. |
| P3 | Export format | Export/delete controls include training block persistence and progression tables, but export is still a preview/count flow. | Users will eventually need a portable artifact. | Add downloadable export bundle after data model settles. | Non-blocking. |
