# Known Gaps

| Priority | Domain | Issue | Why it matters | Recommended next action | Blocking |
| --- | --- | --- | --- | --- | --- |
| P1 | EAS build artifact | EAS project `@karlcupid/corneriq` is linked in `app.json`. Android `preview` build `d550e9bb-b705-41a3-bae7-76c2b6d38453` failed in Gradle/Hermes because the floating Supabase dependency resolved to a Hermes-incompatible dynamic import. Supabase is now pinned to `2.50.0`, Expo/Metro checks pass, and fresh cache-cleared build `c21c5692-011e-4c85-949f-355d0e1f753f` finished with APK artifact `https://expo.dev/artifacts/eas/pYeMLGCyyhfB72dRYhG93K.apk`. | CornerIQ has an artifact lane, but external distribution still needs private access controls, physical-device checks, and metadata acceptance. | Record the artifact in release handoff, distribute only through a private owner-approved channel, and complete release-owner physical-device/store-metadata checks before inviting external users. | Not blocking local audit completion; blocking external rollout until release-owner distribution controls are confirmed. |
| P1 | Human boxer validation | First rough Windows/Chrome browser test found onboarding and Today first-run clarity issues; subsequent launch-UX work addressed confusing labels, raw fields, weekly boxing-session framing, quick-log scale copy, Start here guidance, explicit risk-first Today action routing, fatigue-first Today hierarchy, compact Fuel `Do now` status, logging-first Train/Fuel paths with a return-to-overview control, weekly-intent Plan, athlete-first Plan Details, Profile Data Danger Zone, outside-app support copy, and reduced default disclosures across Today/Fuel/Train/Plan. | IA and friction choices still need validation under real training-day fatigue, weigh-in pressure, mobile keyboards, quick-log interruptions, and Today-to-Train/Fuel routing. | Rerun the first-time browser test from sign-up through Today, then run guided mobile sessions on Today, Fuel Command, Train Workout, Plan Week/Next Week/Adjustments, Profile Data/Safety, and the fast workout completion path. | Blocking for confident launch UX, not for current local smoke. |
| P1 | Coach activation boundary | The Edge Function now requires a Bearer JWT, verifies the caller with Supabase Auth, and only lets the athlete activate a pending relationship, but production audit logging/admin policy and UI gating are still incomplete. | Coach-only commands must not ship broadly until active status, consent, permissions, and audit behavior are proven end to end. | Add audit events, deployed function tests, admin/team policy, and athlete-facing consent copy before exposing coach UI. | Blocking for shared coach accounts. |
| P1 | Permissioned nutrition review workflow | Nutrition safety reviews now persist rows/events, athlete acknowledgement, precise statuses, centralized transition rules, and a trusted-server Edge Function skeleton, but there is no coach/clinician/dietitian reviewer UI, no messaging, and no deployed reviewer-clear operation. | Safety stops are visible, persisted, and cannot be self-cleared. Real review operations still need active relationship lookup, reviewer identity, permission policy, and audit persistence wired server-side. | Wire reviewer relationship lookup and audit event writes in the server function before exposing `cleared_by_reviewer` or reviewer UI. | Blocking for production review workflow, not for current safety copy. |
| P1 | Remote release evidence | Local gates, stricter coverage thresholds, release-quality workflow/script, generated exact-SHA evidence, evidence registry coverage, support-boundary docs, and migration static tests are in place, but remote Supabase migrations `010` through `012` are pending unless generated evidence proves otherwise, live smoke is blocked, and Release Quality cannot pass without current generated evidence. | Local production-quality readiness is stronger, but release evidence must prove the current remote schema and live path before handoff. | Apply or verify remote migrations `010`-`012`, rerun migration list/dry-run until aligned, run live smoke with non-secret evidence, rerun `npm run release:evidence` and `npm run release:quality`, and keep generated proof under `qa-artifacts/`. | Blocking for production release evidence; not blocking local code audit. |
| P1 | Apple account deletion deployment | The app and repo now include Profile > Data > Delete account, a typed client call, and `supabase/functions/delete-account`, but the production Supabase project still needs deployment and non-secret smoke evidence. | Apple requires account deletion initiation inside the app, and the server-side trust boundary must exist in the submitted production environment. | Deploy `delete-account`, confirm `SUPABASE_URL` and server-side role key are configured only in function env, smoke-test with a review account, and record non-secret pass/fail in release evidence. | Blocking Apple submission until deployed and smoke-tested. |
| P2 | Public legal/support URLs | Public Privacy Policy and Support pages are published and configured as app release-link defaults. The release owner still needs to enter the same URLs in App Store Connect and rerun Apple-submission preflight after the remaining assets are ready. | Apple reviewers need public legal/support URLs in metadata and accessible inside the app. | Use `https://sites.google.com/view/corneriq/privacy-policy` and `https://sites.google.com/view/corneriq/support` in App Store Connect; override the public env values only if the pages move. | Non-blocking for in-app URL access; App Store Connect metadata remains tracked under distribution assets. |
| P1 | Team memberships | `coach_team_memberships` was deferred. | A coach/team model will eventually need more than one-to-one athlete-coach rows. | Add team membership tables only after the one-to-one active relationship and permission model is proven. | Blocking for team workflows, not athlete-only use. |
| P2 | Numeric load progression | Optional structured actual fields now persist and summarize, while free-text `loadText` remains notes only and is not parsed. Automatic load prescription progression is still deferred. | Avoids fake progress and unsafe assumptions, but strength progression remains conservative until enough structured data exists. | Collect structured actuals, add calibration tests, then enable load-prescription progression only from explicit fields. | Non-blocking. |
| P2 | Generated-session catalog depth | Accepted previews now materialize conservative future support sessions, but the mapping intentionally uses a small safe family set. | The durable future week is workout-ready, but advanced support variety should not be added without more catalog validation. | Expand family-specific templates only with prohibited-term tests, hard-day-cap tests, and no numeric load inference. | Non-blocking. |
| P2 | Mobile UX polish | Onboarding, Today, quick logs, Fuel, Train, Plan, and Profile Data now have clearer first-run copy, labels, examples, weekly boxing-session chips, fatigue-first ordering, collapsed technical/destructive detail, and success feedback, but they still need hands-on mobile validation. | Phone users will interact between sessions; dense input flows and tab-to-tab routing can reduce logging consistency if they do not match real training-day behavior. | Observe real users, then split the densest forms into sheets/steppers only if real friction remains, without moving logic into screens. | Non-blocking. |
| P2 | Agent QA loop limitations | Approved local Playwright agent QA covers first launch, full onboarding, Today, Fuel, Train, Plan, Profile Safety, Profile Data controls, error/recovery source checks, engine-output review, page-text snapshots, deterministic analysis, contact sheet, and a shareable bundle without Supabase credentials. It still uses deterministic local E2E state and does not validate live auth, real account data, email confirmation, physical devices, distribution artifacts, or human safety interpretation. | The harness reduces manual documentation work, but it must not be mistaken for real launch validation or release approval. | Use `cmd /c npm run qa:agent:ci` for repeatable evidence, share `qa-artifacts/corneriq-agent-qa-bundle.zip` for AI review, then schedule human review for live Supabase auth, physical mobile testing, boxing safety copy, cycle privacy, and release-owner distribution checks. | Non-blocking for local audit automation; blocking for release confidence if used as the only QA evidence. |
| P2 | Support/incident intake | In-app feedback and issue-reporting surfaces are removed from launch runtime, migration `012` drops the old feedback table, and Profile/error states now tell testers to contact support outside the app without sending emergency medical information. There is no production issue triage dashboard or admin-reviewed in-app queue. | Real user support needs a privacy-reviewed intake path, but launch runtime should not collect free-form health details, secrets, or emergency reports. | Keep support intake outside the app until admin/reviewer boundaries, retention policy, privacy copy, and permission rules are defined. | Non-blocking for local launch-code audit; blocking for in-app support workflows. |
| P2 | Production release operations | Local release operations, CI quality checks, Release Quality workflow/script, app recovery, scenario QA docs, EAS profiles, production preflight, coverage, focused smoke, agent QA, production quality audit template, generated release evidence artifact, static safety scans, and a successful EAS artifact lane are in place. | Production rollout still needs remote migrations `010`-`012`, live smoke, store metadata, and owner-run mobile checks. | Run the manual Release Quality workflow after Supabase migrations are aligned and live smoke is recorded; keep mobile distribution in the release-owner lane and exact-SHA proof in generated artifacts. | Non-blocking for local launch-code audit; blocking for external release evidence. |
| P1 | App distribution assets and metadata | App name, slug, version, bundle identifier, Android package, EAS project link, EAS profiles, an APK artifact, and public Privacy/Support URLs exist, but final app icon, splash, screenshots, App Review notes, and App Store Connect metadata have not been completed. `ios.supportsTablet` is false for MVP until iPad validation exists. | Apple submission should not look ready while required assets and metadata are unfinished. | Complete `docs/release/APPLE_REVIEW_HANDOFF.md`, wire final icon/splash in `app.json`, capture screenshots, fill App Store Connect metadata with the public URLs, and rerun Apple-submission preflight. | Blocking Apple submission. |
| P2 | Youth/minor support | Public MVP onboarding now blocks under-18 setup, while engine minor safety gates remain for future policy and test coverage. | Youth athlete support needs guardian consent, age-appropriate privacy handling, and additional review before collecting sensitive body, cycle, nutrition, or safety data. | Keep MVP marketing 18+ and do not select Kids category. Revisit youth workflows only after guardian/privacy/legal policy is ready. | Non-blocking for 18+ MVP; blocking youth/minor launch. |
| P2 | External analytics | No external analytics or event pipeline is connected. | Privacy is simpler, but aggregate usage patterns are not available outside smoke/tests and direct support. | Decide whether privacy-safe analytics are needed after guided human validation proves the core flows. | Non-blocking. |
| P2 | Routed drilldowns | Fuel, exercise, block, and safety histories have stateful detail views inside their panels, but there are no dedicated routed drilldown screens yet. | Detail-on-demand is enough for launch-code comprehension, but longer histories need dedicated space and comparison views. | Add routed, view-model-driven Fuel history, exercise history, block history, and safety screens after user testing. | Non-blocking. |
| P2 | Plan adjustment UX | Plan controls are sectioned and athlete-only in UI; copy now frames buttons as engine requests and rejected/review-needed results show risk explanations. Coach move controls are not exposed and drag/drop remains deferred. | The command model is safer, but repeated weekly planning may still need clearer interaction design after human testing. | Add date picking or a safer move-session flow only after coach permissions, preview persistence, and human findings justify it. | Non-blocking. |
| P2 | Scheduled/background roll-forward | App refresh now auto-materializes accepted previews at the boundary, but no server-scheduled job exists for athletes who do not open the app that day. | App-refresh automation covers normal use, but a server job may later improve durability for long gaps between sessions. | Decide whether to add a scheduled Supabase job after production audit/event policy is settled. | Non-blocking. |
| P2 | Nutrition command snapshot table | Fuel command snapshots are persisted through existing `nutrition_targets.target_payload`; review rows now have dedicated tables, but command snapshots still do not. | Current audit is adequate, but deeper command diff/history views would benefit from a purpose-built table separate from nutrition targets. | Add additive migration only when command history or reporting requires queryable nutrition-command records. | Non-blocking. |
| P2 | Food logging depth | Food logging remains quick manual macro/fiber/sodium entry with grouped Fuel history and day-level detail only. Barcode scanning, full meal planning, and a detailed food database remain intentionally deferred. | Command confidence can improve from logs, but athletes cannot yet use a routed detailed meal history or food database. | Add routed manual Fuel history only after review permissions settle; keep barcode scanning and meal planning deferred. | Non-blocking. |
| P2 | Guided workout-player validation | Train now has a confined full-screen guided player with engine-owned setup, work, rest, transition/checkpoint, and cooldown steps. The live surface shows section, progress, big timer, current step, exact instruction, one cue, intent, visible stop rule, next up, pause/restart/skip/pain controls, substitutions, finish summary, and result persistence. The previous dead workout-options control has been removed. Self-paced strength sets wait for `Done`; rest and timed boxing steps can advance from their timers. `Quick log` remains available for tired users, and optional exercise details/extra fields remain collapsed. In-progress follow-along state is in-memory only while the app shell remains mounted and the UI now warns that reload/discard can lose progress. | The player is now player-ready in code, but real boxer comprehension, in-memory resume expectations, and post-workout entry friction still need mobile validation. | Observe real training-day use; persist in-progress workouts only if users need reload recovery, and split optional exercise logging into a lighter sheet or stepper later while preserving blank-row defaults. | Non-blocking. |
| P2 | Cycle longitudinal UX | Cycle effects are explicit in training decisions, next-week preview blockers, weekly summary flags, and private longitudinal summary copy, but history remains card-level. | Symptom-aware adjustments now have a safer summary, but longer-term review will need a dedicated private trend surface after user testing. | Add a routed/private cycle trend screen only if real users need more detail. | Non-blocking. |
| P3 | Calendar polish | Plan has no drag/drop or calendar package. | Weekly command center is sectioned and readable but not yet a full planner. | Add after preview materialization, coach permissions, permissioned adjustment commands, and boxer user validation are hardened. | Non-blocking. |
| P3 | Export format | Export/delete controls now generate a redacted portable JSON bundle in-app, but platform share/file-save polish is still deferred. | Users can inspect and copy a portable artifact, but native save/share may be expected later. | Add platform share/file-save after Apple submission blockers and data model stability are resolved. | Non-blocking. |
