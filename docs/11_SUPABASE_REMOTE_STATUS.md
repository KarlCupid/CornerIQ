# Supabase Remote Status

Date: 2026-06-19

Purpose: stable runbook and historical status for CornerIQ Supabase release evidence. Exact current-candidate proof belongs in `qa-artifacts/release-evidence/current-release-evidence.md`, generated by `npm run release:evidence` and validated by `npm run release:quality`.

This document must not be treated as proof that the linked remote database is aligned for a future commit.

## 2026-07-24 Generated Workout Regeneration Hotfix

The release owner explicitly approved a production fix after TestFlight exposed a duplicate-key failure while replacing stale generated workout content. The existing history-wide idempotency index was preserved for compatibility with installed clients. Migration `20260724203621_archive_superseded_generated_session_keys.sql` instead adds a hardened trigger that archives the superseded row's key before uniqueness checks, allowing the canonical replacement to be inserted without deleting workout history.

| Evidence | Production status |
| --- | --- |
| Pre-apply dry-run | Reported only `20260724203621_archive_superseded_generated_session_keys.sql` pending. |
| Production apply | Migration `20260724203621` applied successfully to project `fohdypahnobcchfmcrrn`. |
| Schema verification | Trigger exists; function is `SECURITY INVOKER` with an empty `search_path`; legacy compatibility index remains present. |
| Behavior verification | A transaction-scoped temporary-table probe returned `trigger_behavior_verified = true` and rolled back. No user rows were created or modified by the probe. |
| Security advisor | No new migration-specific security finding; the existing leaked-password-protection warning remains. |
| Local verification | Typecheck, lint, production preflight, and 922 tests passed. |

The app repository also archives the key explicitly for new clients, so the database trigger is a backward-compatible safeguard for already-installed TestFlight builds.

## 2026-07-23 Production Smoke And Pending Fix

An explicit opt-in smoke used the dedicated review account against the production project (`fohdypahnobcchfmcrrn`). Authentication succeeded and the test reached authenticated reads, manual-log persistence, journey loading, and deterministic engine projection. It then failed before completing the full workflow for two concrete compatibility reasons:

- Postgres timestamp text from existing production rows was passed directly into strict ISO date-time schemas by several repository mappers.
- The engine's valid `conservative_start` no-plan preview strategy was missing from the production `training_next_week_previews_volume_strategy_known` constraint.

The smoke test's guarded cleanup ran. No credentials or secret values were printed or stored.

The local fix normalizes database timestamps at repository boundaries and adds `20260723233725_align_next_week_volume_strategy.sql`. Focused typecheck and repository/migration tests pass. A linked dry-run reports that this new migration is the only pending production migration. The migration has **not** been applied: production remains unchanged pending explicit release-owner approval, after which the live smoke must be rerun to completion.

## 2026-07-18 Development Migration Verification

The workspace was confirmed linked to the active `CornerIQ Development` project (`llsmdsraunsweqmvefhj`). The production `CornerIQ` project (`fohdypahnobcchfmcrrn`) was not targeted or modified.

| Evidence | Development status |
| --- | --- |
| Pre-apply migration list | `20260718092403_remove_obsolete_medical_profile_data.sql` was the only local migration missing remotely. |
| Pre-apply dry-run | Reported that only `20260718092403_remove_obsolete_medical_profile_data.sql` would be pushed. |
| Development apply | Applied migration `20260718092403` successfully. |
| Migration history query | Returned one applied history row for version `20260718092403`. |
| Data/schema verification query | Returned `profiles_with_obsolete_fields = 0` and `obsolete_column_count = 0`. |
| Linked schema lint | Passed with no errors in `extensions` or `public`. |
| Post-apply dry-run | Reported `Remote database is up to date.` |

This verifies the Phase 1 questionnaire cleanup against development Supabase without requiring Docker Desktop. Production remains pending explicit release-owner approval and fresh exact-candidate checks.

## Project Link

- Supabase CLI local dev dependency: `supabase@2.100.1`.
- Linked project ref previously recorded: `fohdypahnobcchfmcrrn`.
- Local CLI link metadata: `supabase/.temp/`, ignored by git.
- Ignored local credentials: `.env`, ignored by git.
- No secret values are stored in this document.

## Historical Remote Migration Status

Historical remote evidence before migration `010` showed migrations `001` through `009` applied remotely and a dry-run clean for that older migration set.

Later historical evidence after local migration `010` was added showed:

| Evidence | Historical status |
| --- | --- |
| Supabase CLI version | `2.100.1`. |
| Local migration file | Present: `supabase/migrations/010_generated_sessions_training_block_scope.sql`. |
| Local tests | Present in service/static coverage for generated-session active-block scope. |
| Remote migration list | Connected to remote and showed local `010` with no matching remote migration. |
| Remote dry-run | Reported it would push `010_generated_sessions_training_block_scope.sql`; no migration was applied. |

On 2026-06-18, an explicit production maintenance pass updated the linked project `fohdypahnobcchfmcrrn`:

| Evidence | Historical 2026-06-18 status |
| --- | --- |
| Supabase CLI version | Local dev dependency `supabase@2.100.1`; CLI noted a newer version was available. |
| Remote migrations | Local and remote migration lists were aligned for migrations `001` through `013`. |
| Remote dry-run | `cmd /c npm exec supabase -- db push --dry-run` reported: `Remote database is up to date.` |
| Account deletion Edge Function | `delete-account` listed ACTIVE, version `2`, updated `2026-06-18 22:30:16 UTC`. |
| Live account deletion smoke | A confirmed signed-in account completed Profile > Data > Delete account, signed out, and the deleted credentials could no longer sign in. |

The deleted smoke account must not be reused for App Store review. Create and confirm a fresh review account before submission.

Current local migration files now continue beyond that historical remote pass:

- `014_temporal_integrity_session_resolution.sql`
- `20260619190201_training_week_finalization_authority.sql`
- `20260619194631_generated_session_identity_lifecycle.sql`

The later migrations were not aligned until the guarded 2026-06-19 release pass below. Future candidates still need generated exact-SHA migration list and dry-run evidence.

On 2026-06-19, non-mutating linked remote checks confirmed the pending state:

| Evidence | 2026-06-19 status |
| --- | --- |
| `cmd /c npm exec supabase -- migration list` | Remote entries are present through `013`; local `014`, `20260619190201`, and `20260619194631` have no remote entries. |
| `cmd /c npm exec supabase -- db push --dry-run` | Would push `014_temporal_integrity_session_resolution.sql`, `20260619190201_training_week_finalization_authority.sql`, and `20260619194631_generated_session_identity_lifecycle.sql`; no migration was applied. |
| `cmd /c npm exec supabase -- db lint --linked --level error --fail-on error` | Passed for the currently linked remote schema. This does not validate the pending migrations. |

On the same date, local clean-database evidence was produced without mutating the linked remote:

| Evidence | 2026-06-19 local status |
| --- | --- |
| `cmd /c npm exec supabase -- start --exclude edge-runtime,gotrue,imgproxy,kong,logflare,mailpit,postgres-meta,postgrest,realtime,storage-api,studio,supavisor,vector` | Started local Supabase database only and applied migrations `001` through `014`, `20260619190201`, and `20260619194631` in order. |
| `cmd /c npm exec supabase -- migration list --local` | Local migration history showed every local migration present through `20260619194631`. |
| `cmd /c npm exec supabase -- db lint --local --level error --fail-on error` | Passed with no schema errors. |
| `cmd /c npm exec supabase -- gen types typescript --local` | Generated fresh local database types; `src/services/supabase/database.types.ts` was regenerated from that clean local schema. |
| `cmd /c npx vitest run src/tests/static/supabaseMigrationStatic.test.ts` | Passed; static checks now guard launch migrations for lifecycle constraints, generated type columns, and RLS/Data API grant review on future public tables. |

This local evidence proves the pending migrations apply and lint locally. It does not prove the linked remote or staging database has been migrated, nor does it replace live smoke or cross-user RLS checks.

Later on 2026-06-19, the release owner explicitly requested remote release checks. The guarded release path loaded ignored local credentials without printing values, set `CORNERIQ_ALLOW_REMOTE_DB_PUSH=1`, applied the pending migrations, and verified the linked remote:

| Evidence | 2026-06-19 guarded remote status |
| --- | --- |
| `cmd /c node scripts/collect-release-evidence-input.mjs` with guarded remote push enabled | Applied pending migrations before final verification. |
| `cmd /c npm exec supabase -- migration list` | Local and remote entries are aligned for every migration from `001` through `014`, `20260619190201`, and `20260619194631`. |
| `cmd /c npm exec supabase -- db push --dry-run` | Reported `Remote database is up to date.` |
| `cmd /c npm exec supabase -- db lint --linked --level error --fail-on error` | Passed with no schema errors. |
| `cmd /c npm run smoke:live-db` with `CORNERIQ_LIVE_DB_SMOKE=1` | Failed at sign-in with Supabase `invalid_credentials`; env names were present, but the configured smoke account credentials were not accepted. No rows-created/cleaned evidence was produced. |

Remote migration alignment is no longer the active blocker for this pass. Live smoke remains blocked until a valid confirmed smoke account is supplied and `cmd /c npm run smoke:live-db` passes.

## Required Current-Candidate Evidence

For each release candidate, record non-secret results in generated release evidence:

- Candidate SHA and short SHA.
- Date/time.
- `cmd /c npm exec supabase -- --version`.
- `cmd /c npm exec supabase -- migration list`.
- `cmd /c npm exec supabase -- db push --dry-run`.
- Local or staging clean-database migration apply result.
- `cmd /c npm exec supabase -- db lint --local --level error --fail-on error` or equivalent staging lint.
- Generated TypeScript database type validation result.
- Whether every local migration file under `supabase/migrations/` is applied remotely or still pending, including `014_temporal_integrity_session_resolution.sql`, `20260619190201_training_week_finalization_authority.sql`, and `20260619194631_generated_session_identity_lifecycle.sql`.
- Whether the `delete-account` Edge Function is deployed and smoke-tested for full in-app account deletion.
- Exact blocker if credentials or permissions are unavailable.

Do not treat the 2026-06-18 historical pass as proof for a future release candidate after additional migrations, function changes, or config changes. Generated evidence must record a fresh migration list and dry-run for the exact candidate SHA.

## Remote Mutation Guard

Applying remote migrations is release-owner work. Before mutating the remote database:

- Confirm release-owner credentials and permissions are available.
- Require explicit local opt-in: `CORNERIQ_ALLOW_REMOTE_DB_PUSH=1`.
- Do not print access tokens, DB passwords, service-role keys, smoke credentials, or personal emails.
- After any apply, rerun migration list and dry-run and record only non-secret results.

Without that guard, do not apply remote changes. If fresh exact-candidate evidence is unavailable, treat remote alignment as unverified for that release candidate.

## Live Smoke Status

The production account-deletion smoke passed on 2026-06-18 after remote migration alignment and the CORS-enabled `delete-account` v2 deployment. A fresh generated release evidence artifact should still record live-smoke status for each final candidate.

Required non-secret evidence when a release owner runs it:

- Date/time.
- Candidate SHA.
- Command: `cmd /c npm run smoke:live-db` with `CORNERIQ_LIVE_DB_SMOKE=1`.
- Public URL env present: yes/no.
- Public anon key env present: yes/no.
- Public env names: `EXPO_PUBLIC_SUPABASE_URL` and `EXPO_PUBLIC_SUPABASE_ANON_KEY`.
- Smoke email/password env names present: yes/no, without values.
- Pass/fail.
- Rows created and cleaned summary.

The regular suite still includes `src/tests/live/liveDbSmoke.test.ts`; it skips unless `CORNERIQ_LIVE_DB_SMOKE=1` is set.

Account-deletion smoke evidence should additionally record the non-secret result of:

- Sign in with a confirmed production review/smoke account.
- Open Profile > Data > Danger Zone.
- Type `DELETE ACCOUNT` and confirm.
- Verify the app signs out.
- Verify the deleted credentials can no longer sign in.

## Secrets

- No service role key is used in Expo/client code.
- Edge Functions reference `SUPABASE_SERVICE_ROLE_KEY` only through function env.
- No smoke email or password value should be printed into docs.
- No secret values should be written into tracked files.
- `.env` remains ignored by git.
