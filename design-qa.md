Current premium dark-glass fidelity audit:

- `docs/qa/PREMIUM_DARK_GLASS_FIDELITY_AUDIT.md`

The previous "no issues remain" note is superseded. The current second-pass finding is that the shared UI is closer, but 1:1 fidelity still depends on regenerated photographic screen backgrounds/hero crops, a lean vector rendering dependency, and fresh mobile portrait comparison captures after local onboarding reaches the tab screens.

Icon/splash files are wired in `app.json`; this audit does not prove final App Store artwork acceptance.
